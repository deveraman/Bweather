name: openweather_api
description: A starting point for Dart libraries or applications.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  build_runner: ^2.3.3
  dio_provider:
    path: ../dio_provider/
  envied: ^0.3.0
  openweather_repository:
    path: ../openweather_repository/

dev_dependencies:
  envied_generator: ^0.3.0
  lint: ^2.0.1
  test: ^1.16.0
